DROP TABLE IF EXISTS SkiDestinations ;

CREATE TABLE IF NOT EXISTS SkiDestinations (
  DestID 		INT 			NOT NULL,
  Latitude	 	DECIMAL(10,10) 	NOT NULL,
  Longitude 	DECIMAL(10,10) 	NOT NULL,
  DriveTime 	VARCHAR(45) 	NOT NULL,
  PRIMARY KEY (DestID)
  )